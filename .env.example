# Copy to .env for docker compose. These are passed into the container.
# Required:
GEMINI_API_KEY=
TAVILY_API_KEY=
# Optional (defaults shown):
DEFAULT_MODEL=gemini-3.1-flash-lite
# Optional: bridge each agent's built-in bus to a remote bus server.
# SSR_BUS_URL=ws://127.0.0.1:8765
